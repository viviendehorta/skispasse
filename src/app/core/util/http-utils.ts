import {HttpHeaders} from "@angular/common/http";

export const JSON_REQUEST_HEADER_OPTIONS = {
    headers: new HttpHeaders({
        "Content-Type": "application/json"
    })
};

export class HttpUtils {
}
